machine:
  python:
    version: 2.7.5
dependencies:
  pre:
    - "pip install --upgrade pip"
    - "pip install pybuilder"
    - "pyb install_dependencies"
test:
  override:
    - "pyb run_unit_tests"